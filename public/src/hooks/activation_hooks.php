<?php

namespace WebAvifConverter\Hooks;

function activate_web_avif_converter()
{
    // This plugin doesn't need activation hooks, as it doesn't add any database tables or settings on activation.
}

function deactivate_web_avif_converter()
{
    // This plugin doesn't need deactivation hooks.
}

